# Claude Code local session data
.claude/

# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# Distribution / packaging
*.egg-info/
dist/
build/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Mac
.DS_Store

# Environment variables
.env
.env.local

# Private data files (exclude real data)
data/graphs/*.pkl
data/graphs/system_graph_data.json
data/filtering/filtered_nodes_review.json
data/filtering/blacklist.txt
data/filtering/whitelist.txt

# Exclude all question data (keep only examples)
data/questions/*.json
data/sample_questions_for_audit.json

# Audit data (human review results - keep safe, don't commit)
data/audit_state.json
data/audit_results/
data/flagged_questions.jsonl

# But include example files
!data/graphs/example_graph.pkl
!data/filtering/example_filtered_nodes.json
!data/filtering/example_blacklist.txt
!data/filtering/example_whitelist.txt
!data/questions/example_questions.json

# Archived one-off scripts and old question drafts (not tracked)
scripts/archive/
data/questions/archive/

# Reference code (internal)
reference_code/

# Evaluation results and logs (generated during runs)
logs/
results/cache/
results/*/

# Paper writing workspace (FRI submission material — not for public repo)
paper/
Manuscript_FlavorReasonBench.pdf
Descriptor_graph-LLMs-figures.pptx

# Ad-hoc analysis and figure-generation scripts (results reproduced via Zenodo notebook)
scripts/analyze_*.py
scripts/generate_*.py
scripts/apply_id_rename_to_tex.py
scripts/_model_style.py

# Citation PDFs and reference files (not for public repo)
data/citations/
data/backups/
data/resolved_models.json

# Experiment outputs
results/
